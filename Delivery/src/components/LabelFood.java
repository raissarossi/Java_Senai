package components;

import javax.swing.*;
import java.awt.*;
public class LabelFood extends JButton{
    public LabelFood(Color color, int x, int y, int w, int h){
        setBackground(color);
        setBounds(x,y,w,h);
//        setVisible(true);
    }
}
